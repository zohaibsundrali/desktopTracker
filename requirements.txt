customtkinter==5.2.0
supabase==1.1.1
# Session lifecycle adapter is verified against this GoTrue local cleanup API.
gotrue==1.3.1
# Isolated bounded logout requests (matches the Supabase 1.x dependency).
httpx==0.24.1
pyautogui==0.9.54
pynput==1.7.6
psutil==5.9.6
pandas==2.2.3
numpy==2.1.3
pillow==11.0.0
python-dotenv==1.0.0

# Windows-only: reads the foreground window/app + process name. REQUIRED for
# application and per-website tracking on Windows (app_monitor uses win32gui /
# win32process). Without it the app runs but captures no app/website usage.
pywin32>=306; platform_system == "Windows"

# Windows-only: reads the browser address bar for per-website tracking.
# The app still works without it (falls back to tab-title detection).
uiautomation>=2.0; platform_system == "Windows"
